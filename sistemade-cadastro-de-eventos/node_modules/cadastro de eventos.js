const { getRawInput } = require("readline-sync");

console.log('Bem vindo ao Cadastro de Eventos');
let dataAtual=new Date();
let dataEvento=prompt('Insira a data do evento');
if (dataEvento<dataAtual) {
    console.log('Data inválda');
} else {
    prompt('Você é palestrante ou participante?');
}
let palestrante=prompt('Qual é o seu nome?');
console.log('Palestrante cadastrado');
if (participante) {
    let participante=prompt('Qual é o seu nome?');
    let listaParticipantes=(participante.length>listaParticipantes++);
}
if (listaParticipantes>100) {
    console.log('Cadastro não efetuado. Evento lotado!');
} else {
    prompt('Qual é a sua data de nascimento?');
    let dataNascimento=prompt('insira sua data de nascimento');
    if (dataAtual-dataNascimento>=18) {
        console.log('Participante cadastrado. Bom evento!');
    } else {
        console.log('Evento proibido para menores!');
    }
}
